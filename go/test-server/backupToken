package token

import (
    "fmt"
	"strconv"
    "net/http"
	"encoding/json"
)

func main() {
    http.HandleFunc("/", func (w http.ResponseWriter, r *http.Request) {
        fmt.Fprintf(w, "This is the token server")
    })
    http.HandleFunc("/memes", func (w http.ResponseWriter, r *http.Request) {
        
    })
}